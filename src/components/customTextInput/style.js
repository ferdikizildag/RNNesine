import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    height: 40,
    borderColor: 'gray',
    borderWidth: 1,
    marginBottom: 20,
  },
  label: {
    marginBottom: 5,
    marginTop: 10,
  },
});
